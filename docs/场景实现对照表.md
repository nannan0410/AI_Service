# 场景实现对照表

> **更新日期**：2026-07-22  
> **依据**：落地计划 §八 / §九、`docs/项目现状.md`、当前 Skill / Workflow / Mock 代码  
> **说明**：落地计划 §九 部分 Skill「未启用」状态已过时，**以实现代码为准**。

---

## 一、总览：规划 vs 实现

| 阶段 | 规划功能点 | Skill（规划） | 实现状态 | 实际 Skill / Workflow |
|------|------------|---------------|----------|----------------------|
| **游前** | 欢迎 + 规则推荐入口 | `proactive_marketing` | ✅ 部分 | RecommendEntry + `welcome_questions`（规则）+ `welcome_templates`（标题）；Hero 天气 Mock |
| **游前** | 新客领券 | （营销子集） | ✅ | `newGuestCoupon` Workflow（独立于 Skill 列表） |
| **游前** | 智能购票全流程 | `ticket_purchase` | ✅ | `ticket_purchase` |
| **游前** | 等级+折扣+券+标签商品推荐 | `member_offer` | ✅ | 独立 Skill（非 `scenic_recommend`）；奇趣乐园票+VIP 券；游游推荐 `vip_member_offer` |
| **游前** | 游玩攻略（交通/入园/项目） | `travel_guide` | ✅ | `travel_guide`（含多子意图） |
| **游前** | OTA/TA 订单只读查询 | `order_query` | ✅ | `order_query` |
| **游前** | 查券 / 会员券推荐 | `proactive_marketing` | ✅ | `proactive_marketing`（`coupon_query`） |
| **游中** | 二消/商餐推荐 + 券 | `retail_recommend` | ✅ 部分 | 并入 `proactive_marketing`（dining/retail） |
| **游中** | 演出/场次推荐 | `scenic_recommend` | ✅ | `scenic_recommend` → `showSchedule` |
| **游中** | 园内路线（含排队展示） | `travel_guide` / `route_plan` | ✅ 部分 | `travel_guide`（`in_park`）；打卡见下行 |
| **游中** | 园区打卡 + 券/积分 | `checkin_service` | ✅ | `checkin_service` + `/checkin` |
| **游中** | 一键路线 + 打卡 + 券 | `route_plan` | ❌ 部分 | 打卡 MVP ✅；路线串联未做 |
| **游中** | 虚拟排队推荐 | `queue_recommend` | ✅ | 在园；免费包/付费单/点名；`/queue/take` `/queue/pay` |
| **游中** | 答题互动 | `quiz_interact`（挂靠） | ✅ | 无独立「我想答题」；演出/明星结果 CTA；海洋公园 3 套题 |
| **游中** | 失物招领 | `lost_found` | ❌ | **不做** |
| **游中/后** | 停车缴费 | `parking_pay` | ✅ | `parking_pay` + `/parking` |
| **游后当日** | OCR 小票积分 | `receipt_points` | ❌ | **P3 预留**（`/receipt` 页有，未接对话） |
| **游后当日** | 离园推送（发票/点评/复游券） | — | ❌ | **P3 预留** |
| **游后非当日** | 服务点评 | `review_service` | ✅ | `review_service` |
| **游后非当日** | 批量开发票 | `invoice_service` | ✅ | `invoice_service` |
| **游后** | 推荐好友 | — | ❌ | **P3 预留** |
| **游后** | 快递周边发券 | — | ❌ | **不做** |
| **推券** | 欢迎态主动气泡（H2） | — | ❌ | **P3 预留** |
| **推券** | 游中地理围栏 / 离园推券（H5） | — | ❌ | **P3 预留** |

---

## 二、已实现：细分场景 + 判断条件 + Skill + 展示字段 + 关键判断字段

### 2.1 游前

| 场景 | 细分 | 触发判断条件（摘要） | Skill / Workflow | UI 展示字段（Mock） | 关键判断字段 |
|------|------|----------------------|------------------|---------------------|--------------|
| **欢迎推荐** | 快捷服务 chips | RecommendEntry 规则过滤 + 优先级 | （规则引擎，非 Skill） | `title`、`icon`、`target`/`promptHint` | `personaId`、`hasPendingVisitOrder`、`hasVisitToday`、`inPark`、`canClaimNewGuestCoupon`、`hasInvoiceableOrders`、`tags`、`visitorPhase` |
| **欢迎推荐** | 游游推荐问题 | `welcome_questions` + `evaluateRules` | （规则引擎） | `text`、`prompt`、`icon`、`priority` | 与快捷服务同字段；演示账号多为 `personaId` |
| **游中** | 附近项目推荐入口 | 游游推荐 `inpark_nearby_projects`（`inPark=true`，priority 8，全景区） | `project_query` | `scene_recommend(scene=nearby)`：相对距离 + 在园关怀；定向类「歇脚推荐」可置顶；可并存标签类「因…推荐」 | `visitorState.inPark`；点击 prompt「附近有什么好玩的项目推荐」；分类见《对话意图》§1.2 |
| **欢迎 Hero** | 当日景区天气 + 客流 | 天气按园 Mock；客流三档随机 | — | 天气/客流行在「我是游游」上方 | `scenicWeather.ts` / `scenicCrowd.ts` |
| **游前** | 今天适合游玩吗 | 游游推荐 / 天气人流话术 | `travel_guide`（独立 Workflow） | 文本：天气 + 随机人流 + 建议 | `weatherSuitability.ts` |
| **新客领券** | 成功领取 | 领券意图 + 新客 + 窗口内 + 未持有 | `newGuestCoupon` | Coupon：`title`、`type`、`value`、`condition`、`expireAt` | `personaId=demo_new`、`registeredAt`+`claimWindowDays`、`couponProductId=cp_prod_new`、券 `status` |
| **新客领券** | 已领取 | 账户已有新客券 available | 同上 | 同上 + 文案「已领取」 | `hasNewGuestCoupon` |
| **新客领券** | 非新客 | 非 demo_new | 同上 | 文案 | `personaId` |
| **新客领券** | 超窗口 | 注册超 `claimWindowDays`（产品配置 30，代码默认兜底 7） | 同上 | 文     案 | `registeredAt`、`claimWindowDays` |
| **购票** | 问人数 / 问日期 | 冷启动购票关键词；FAQ「门票怎么用」排除 | `ticket_purchase` | 文案追问 | 会话 `party`、`visitDate` |
| **购票** | 提前推营销券 | 明确购票意图 + 追问阶段 + 未发过营销券 | 同上 | CouponCard（可与问话分条） | 无可用新客券时发 `cp_prod_purchase` |
| **购票** | 推荐票种 | 精确套票→single；纯成人/儿童→single；无套票的成人+儿童→cart | 同上 | **单条** Ticket：`items[]` 多行可选；有营销券时 `offerCoupon` 同框级联 | `ticketTypeId`、`composition`、`offerCoupon` |
| **购票** | 确认下单 | 用户确认 | 同上 | TicketConfirm → OrderCard/`draftId`（可含多行） | `visitDate`、`quantity`/`items`；游客在假页选 |
| **购票** | 不可闭环兜底 | 含老人等无法匹配 | 同上 | TicketFallback + `cp_prod_manual` + `/tickets` | `no_product` |
| **攻略** | 交通指南 | 怎么去/交通/停车位置（非缴费） | `travel_guide` | Content：`title`、`body`（traffic） | `type=traffic`；可选待出行 `visitDate`/`ticketName` |
| **攻略** | 入园提醒 | 入园须知/要带什么/门票怎么用/几点入园 | 同上 | Content：entry_notice | `type=entry_notice` |
| **攻略** | 游玩推荐 | 游玩攻略/玩什么（不含交通入园） | 同上 | Guide + Activity：`name`、`location`、`timeRange`、`tags`、热门提示；**前期隐藏排队** | `scope=recommend`；亲子偏好 `hasChildren` |
| **攻略** | 完整出行攻略 | 出行攻略/按订单做攻略 | 同上 | 交通+入园+项目 | `scope=full`；绑定最近未过期待出行订单 |
| **订单查询** | 自营/OTA/TA | 订单/OTA/第三方 | `order_query` | Order：`orderId`、`ticketName`、`quantity`、`totalAmount`、`status`、`source`、`visitDate` | `source`∈{self,ota,ta}；ota/ta → `readOnly` |
| **查券推荐** | 有什么券/会员券 | 查券/优惠活动关键词；非领新客；非会员选品 | `proactive_marketing` | Coupon 列表：`title`、`value`、`condition`、`expireAt`、`status` | `status=available`；persona 标签可影响推荐文案 |
| **会员权益选品** | 等级+折扣+券+标签 | 「会员专属怎么买最划算」「按会员等级推荐」等 | `member_offer` | Coupon + Ticket（组合价可确认） | `memberLevel`、tags、`scenic_hlg` 票/券 |

### 2.2 游中

| 场景 | 细分 | 触发判断条件（摘要） | Skill / Workflow | UI 展示字段（Mock） | 关键判断字段 |
|------|------|----------------------|------------------|---------------------|--------------|
| **园内路线** | in_park 攻略 | 「在园区内」+「今日路线/排队/先玩哪里」 | `travel_guide` | Activity：`name`、`location`、`waitMinutes`、`queueStatus`、`virtualQueue`、`isHot` | `scope=in_park`；展示排队；`preferences` |
| **餐饮推荐+券** | dining / order_intent_dining | 美食/餐饮…；或「想买」+餐饮语境 | `proactive_marketing` | `scene_recommend`：券 + 餐饮 Activity（**仅在园发券**；非在园只推店铺） | `category=dining`；`inPark` 才发 `cp_prod_dining_guide` |
| **零售推荐+券** | retail / order_intent_retail | 伴手礼/纪念品…；或「想买」+零售 | 同上 | 同上（仅在园发券） | `category=retail`；`inPark` 才发 `cp_prod_retail_guide` |
| **演出推荐** | 当日 | 「今天有演出」「下一场」等 | `scenic_recommend`（**优先于**攻略） | `scene_recommend`：演出卡 + 剩余场次/`showStartTimes` | `dayKind=today`；`category=show` |
| **演出推荐** | 明天/后天 | 文案含明天/后天 | 同上 | 场次复用 + 最早场推荐 | `dayKind=tomorrow/day_after` |
| **演出推荐** | 通用推荐 | 「演出推荐」「有哪些演出项目」等未点日期 | 同上 | 项目推荐文案（非「下一场」逻辑）；不进订单攻略 | `dayKind=general` |
| **停车缴费** | 已绑车牌 / 未绑 | 停车费/交车费（排除停车位置） | `parking_pay` | PageGuide → `/parking`；车牌、费用 | `boundPlateNo` |
| **园区打卡** | 在园取号点 | 「我要打卡」等；附近点可对话内打卡 | `checkin_service` | PageGuide / `/checkin` | `inPark`、`currentLocation`、点位 `location` |
| **虚拟排队** | 免费包 | 「虚拟排队」「排队少的项目」等；**仅在园** | `queue_recommend` | `scene_recommend(scene=queue)`：2 免费卡，每卡「立即取号排队」→ `/queue/take` | `inPark`；`virtualQueue.isFree`；Demo `act_001`+`act_004` |
| **虚拟排队** | 付费单 | 「快速排队」「付费虚拟排队」等；**仅在园** | 同上 | 1 付费卡「¥10元快速排队」→ `/queue/pay` | Demo `act_002`（`queuePrice=10`） |
| **虚拟排队** | 点名项目 | 消息含支持 VQ 的项目名（含简称模糊匹配） | 同上 | 单卡 + 免费/付费对应 CTA；优先于演出/攻略 | `activities.json` `virtualQueue.enabled`；`activityNameMatch` |
| **答题邀请** | 海豚表演 | 演出场次结果含海豚表演且未完成该套题 | 挂靠 `scenic_recommend` | 同条 `scene_recommend` 底部 hint+「开始答题」 | `act_hy_001.quizId`；`completedQuizIds` |
| **明星介绍+答题** | 企鹅 / 白鲸 | 文案含企鹅或白鲸 | `star_intro` Workflow | 同条 `star_intro` 介绍≈200 字 + CTA | `stars.json`；`scenic_hy` |
| **答题作答** | 单题点选 | 点「开始答题」后 QuizCard 选项 | （会话） | 一题一卡；错则停；全对发券+积分 | `quiz.json` 3 套；`cp_prod_hy_quiz` |

### 2.3 游后

| 场景 | 细分 | 触发判断条件（摘要） | Skill / Workflow | UI 展示字段（Mock） | 关键判断字段 |
|------|------|----------------------|------------------|---------------------|--------------|
| **开发票** | 有可开票 | 发票/开票/报销 | `invoice_service` | 「有 X 笔」+「立即开票」→ `/invoice/batch` | `status=completed` 且 `invoiceStatus=none` 且 `completedAt` 在 **90 天内**（按当前景区） |
| **开发票** | 无可开票 | 同上 | 同上 | 「当前没有可申请开票的订单。」 | 过滤结果为空 |
| **服务点评** | 有可评订单 | 点评/评价 | `review_service` | ReviewCard：订单列表、星级、文案、图 | `status=completed` 且 `reviewStatus≠submitted` 且完成 **90 天内** |
| **服务点评** | AI 帮写评价 | 卡片内点「帮我写评价」 | 同上（前端工具，非独立 Skill） | 填入约 40–60 字草稿；可改后提交 | 星级、已选标签、`ticketName`、景区名（`scenic.json`）+ 关键词；有 Key 走 LLM，否则离线模板 |
| **服务点评** | 优质评价奖励 | >20 字 + 2 图 | 同上 | 发点评券（餐饮/停车） | 内容长度、`imageIds` |

---

## 三、规则引擎公共字段（欢迎入口 / RecommendEntry）

`mock/rules.ts` → `buildRuleContext` 产出，供入口显隐：

| 字段 | 含义 |
|------|------|
| `personaId` | `demo_new` / `demo_mid` / `demo_vip` |
| `memberLevel` / `tags` | 会员等级、标签（`field_catalog`） |
| `inPark` | 是否在园 |
| `visitorPhase` | `pre` / `in_park` / `post_later`（由 inPark、可开票/已完成订单推导） |
| `hasPendingVisitOrder` | 是否有未过期待出行（`visitDate`≥今天且 paid） |
| `nextVisitDate` / `upcomingVisitOrderCount` / `hasVisitToday` | 最近出行日、笔数、是否今日出行 |
| `hasInvoiceableOrders` | 90 天内可开票（当前景区） |
| `hasNewGuestCoupon` / `canClaimNewGuestCoupon` | 已有新客券 / 是否可领 |

---

## 四、各实体「该展示」字段速查（Mock）

| 实体 | 建议展示字段（英文字段 · 中文参考） |
|------|--------------------------------------|
| **票产品** | `productId`（产品 ID）、`name`（票种名称）、`price`（售价）、`ticketTypeId`（票种类型）、`composition`（人数构成，如 2大1小）、`tags`（标签）、`channels`（销售渠道）、`status`（上下架） |
| **账户券** | `title`（券名称）、`type`（券类型）、`value`（面额/折扣值）、`condition`（使用条件）、`expireAt`（有效期）、`status`（可用/已用/过期）、（可选）`couponProductId`（券产品 ID）、`redeemActivityName`（兑换项目名） |
| **订单** | `orderId`（订单号）、`ticketName`（票种名称）、`quantity`（人数）、`totalAmount`（实付金额）、`status`（订单状态）、`source`（来源：自营/OTA/TA）、`visitDate`（出行日）、`invoiceStatus`（开票状态）、`reviewStatus`（点评状态）、`completedAt`（核销/完成时间） |
| **游客** | `name`（姓名）、`idType`（证件类型）、`idNumber`（证件号，脱敏）、`gender`（性别） |
| **活动/项目** | `name`（项目名称）、`category`（分类：游乐/演出/餐饮/零售）、`location`（位置）、`timeRange`（开放时段）、`tags`（标签）、`queueStatus`（排队状态）、`waitMinutes`（预计等待分钟）、`isHot`（是否热门）、`showStartTimes`（演出场次）、`virtualQueue`（虚拟排队配置） |
| **内容块** | `type`（内容类型，如交通/入园）、`title`（标题）、`body`（正文） |
| **攻略综合** | `title`（攻略标题）、`visitDate`（出行日）、`ticketName`（关联票种）、`traffic`（交通指南）、`entryNotice`（入园提醒）、`dayPlan`（日程建议）、`activities[]`（推荐项目列表） |

---

## 五、已启用 Skill 与典型关键词（代码）

| skillId | 名称 | 典型触发关键词 / 意图 |
|---------|------|----------------------|
| `ticket_purchase` | 智能购票 | 买票、购票、两大一小、套票、首次购票…（排除门票怎么用/几点入园） |
| `travel_guide` | 游玩攻略 | 交通指南、入园、游玩攻略、今日路线、在园区内… |
| `order_query` | 订单查询 | 订单、OTA、第三方、订票记录… |
| `proactive_marketing` | 主动营销 | 有什么券、美食、纪念品、想买/下单（非门票）… |
| `parking_pay` | 停车缴费 | 停车费、交停车费、绑定车牌…（排除停车位置） |
| `scenic_recommend` | 园区推荐（演出） | 演出、灯光秀、下一场、今天有演出… |
| `member_offer` | 会员权益选品 | 会员专属套餐、按会员等级、适合我的门票组合… |
| `invoice_service` | 发票服务 | 发票、开票、报销、开发票… |
| `review_service` | 服务点评 | 点评、评价、我要点评… |

另：`newGuestCoupon` Workflow（领新客券）由独立意图函数触发，不在 `skills.json` 列表中。

---

## 六、读表说明（避免和落地计划打架）

1. **`scenic_recommend`**：仅演出场次；商品组合推荐已拆为 **`member_offer`**。  
2. **`retail_recommend`**：规划独立 Skill；**实现并入** `proactive_marketing`。  
3. **新客领券**：独立 Workflow，不占用 `proactive_marketing` 主路径。  
4. **虚拟排队**：`queue_recommend` Skill + Workflow；在园；免费包（每卡「立即取号排队」→ `/queue/take`）/ 付费单（「¥10元快速排队」→ `/queue/pay`）/ 点名项目。  
5. **P3 预留**：欢迎态主动气泡推券（H2）、地理围栏/离园推券（H5）、离园主动推送、OCR 小票积分对话接入。  
6. **明确不做**：失物招领、快递周边发券。  
7. **`member_offer`**：会员等级折扣 + 可用券 + 标签选票；演示票/券挂 `scenic_hlg`；欢迎「会员专属怎么买最划算？」仅 demo_vip + 奇趣乐园。  
8. **答题**：仅挂靠演出（海豚表演）/ 明星（企鹅、白鲸）；海洋公园 Mock；不做「我想答题」冷启动；完成一次全对后隐藏 CTA。

---

## 七、相关文档

| 文档 | 用途 |
|------|------|
| [`项目现状.md`](./项目现状.md) | 当前进度与演示脚本 |
| [`AI景区智能聊天助手-落地计划.md`](./AI景区智能聊天助手-落地计划.md) | 全生命周期规划 |
| [`业务场景配置与实现.md`](./业务场景配置与实现.md) | Skill / 入口 / 规则配置 |
| [`对话意图识别与LLM分工.md`](./对话意图识别与LLM分工.md) | NLU 与 Workflow 分流 |
| [`内容数据配置说明.md`](./内容数据配置说明.md) | 攻略 / 活动 Mock |
| [`版本变动说明-v0.2.2.md`](./版本变动说明-v0.2.2.md) | 当前版本（点评 AI 草稿 + 本表） |
| [`版本变动说明-v0.2.1.md`](./版本变动说明-v0.2.1.md) | 上一版能力增量 |
