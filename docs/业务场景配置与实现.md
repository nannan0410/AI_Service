# 业务场景配置与实现

> **文档用途**：Skill / RecommendEntry / 规则引擎 / 意图路由 / 推券场景的**配置架构与已实现规格**  
> **更新日期**：2026-07-22  
> **Phase A**：Skill 配置后台 + field_catalog / card_views  
> **Phase B**：RecommendEntry 编辑 + 规则预览 + 聊天页 merge（2026-06-19 完成）

**相关文档：**

- 改正文（交通/攻略 blocks）：[`内容数据配置说明.md`](./内容数据配置说明.md)
- 总方案基线：[`AI景区智能聊天助手-落地计划.md`](./AI景区智能聊天助手-落地计划.md)
- 当前做到哪：[`项目现状.md`](./项目现状.md)

---

## 1. 问题背景

智能对话工具真实落地时，需要对接后台与底层系统数据。是否需要一个地方维护：

- **触发条件（标签 / 规则）**
- **展示字段项**

**结论：需要系统性地维护，但不建议全部塞进一个配置文件，应分层、分域维护。**

| 维度 | 示例 | 变更频率 |
|------|------|----------|
| 触发条件 | 新客标签、在园、有待出行订单（`visitDate >= 今天`） | 运营 / 产品 |
| 数据能力 | 调哪些 API、字段从哪来 | 后端 / 集成 |
| 展示字段 | 券卡片显示满减条件 | 产品 / 前端 |

---

## 2. 推荐生产架构（四层）

```
① 元数据目录（Tag / Field Catalog）
   ↓ 引用
② 场景配置（Skill / RecommendEntry / welcome_questions / welcome_templates）
   ↓ 调用
③ 数据适配层（Tool Handler + API Binding）
   ↓ 格式化
④ 展示模板（Card Schema / View Config）
```

---

## 2.1 Skill 场景 vs 欢迎页推荐入口

| 维度 | **Skill 场景**（`skills.json`） | **推荐入口**（`recommend_entries.json`） |
|------|--------------------------------|------------------------------------------|
| **定位** | 对话侧的**能力单元**（能做什么） | 面向游客的**运营入口**（推什么、怎么点） |
| **维护者** | 产品 / 技术（能力边界） | 运营 / 产品（文案、规则、优先级） |
| **核心字段** | `triggerKeywords`、`tools`、`promptAddon` | `title`、`icon`、`rules`、`promptHint`、`skillId` |
| **触发方式** | 用户**发消息后**关键词匹配路由 | 欢迎页**展示 chips**，用户点击后发起对话或跳页 |
| **是否含正文** | 否，只定义调哪些 Tool | 否，只有标题与 prompt 提示语 |
| **与用户状态** | 不直接判断（路由看消息文本） | **规则引擎**按 persona / 在园 / 订单等过滤 |
| **一对多关系** | 1 个 Skill 可被多个入口引用 | 多个入口可指向同一 `skillId` |

- **Skill ≈ 实际功能**：Tool 列表、LLM 指令、Workflow。
- **推荐入口 ≈ 展示**：标题、图标、出现时机；点击发 `promptHint` 后进对话路由。

---

## 2.2 欢迎页：「游游推荐」与「快捷服务」

| 维度 | `welcome_questions.json` | `recommend_entries.json` |
|------|--------------------------|---------------------------|
| 展示位置 | 欢迎区 **「游游推荐」**（默认 3 条 + 展开，最多 8 条） | 欢迎区 **「快捷服务」** chips（最多 4 条） |
| 个性化 | 与快捷服务同一套 **规则表达式**（`rules` + `evaluateRules`）；演示账号只是 `personaId` 规则选项之一 | 同左 |
| 后台编辑 | `/admin/business` → 游游推荐 | `/admin/business` → 快捷服务 |
| **优先级** | `priority` **降序**（越大越靠前） | 同左；命中后取前 `MAX_QUICK_SERVICES`（4）条 |
| **当前 Chat 页** | ✅ **已接入** | ✅ **已接入**（Phase C） |

**标题区文案**仍由 `welcome_templates.json` 按 `personaId` 提供（`title` / `subtitle` / `body`），**不含**推荐问题列表。

**Hero 当日天气**（`WelcomeHero`）：在「我是游游」上方展示一行 Mock 文案，默认「今天景区天气 26°C，多云，适合出游」。实现：`src/utils/scenicWeather.ts`（本地常量）；后续可接 `/api/weather/today` 后仍用 `formatScenicWeatherLine`。

**规则求值说明：** 「演示账号」(`personaId`) 按登录账号过滤；「是否在园 / 待出行订单 / 可领新客券」等字段按该演示账号的 Mock 上下文求值（见规则预览）。后台编辑时勿在打开弹层时重置条件值；`personaId` 支持多选（存为 `in`）。

**占位符**（`welcomeTemplateVars.ts`）：`{{nickname}}`、`{{nextVisitDate}}`、`{{upcomingVisitOrderCount}}`、`{{pendingVisitSubtitle}}`。出行日当天 `pendingVisitSubtitle` = **「今日出行」**。

---

## 2.3 关系总览

```
┌─────────────────────────────────────────────────────────────────┐
│ 游客看到的欢迎层（展示）                                          │
├──────────────────────────┬──────────────────────────────────────┤
│ welcome_questions（规则） │ recommend_entries（「快捷服务」chips） │
│ + welcome_templates 文案  │                                      │
└────────────┬─────────────┴──────────────────┬───────────────────┘
             │ 点击发 prompt                     │
             ▼                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│ Workflow 层（代码正则，优先）→ 领券 / 购票 / 攻略 / 订单           │
└────────────┬────────────────────────────────────────────────────┘
             ▼（未命中）
┌─────────────────────────────────────────────────────────────────┐
│ Skill 层：triggerKeywords → LLM + Tool                           │
└────────────┬────────────────────────────────────────────────────┘
             ▼
┌─────────────────────────────────────────────────────────────────┐
│ 内容层（正文）见 内容数据配置说明.md                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2.4 映射示例（当前 JSON）

### Skill → Tool

| skillId | 名称 | 可路由 | 主要 Tool |
|---------|------|--------|-----------|
| `ticket_purchase` | 智能购票 | ✅ | getProductCatalog, createOrderDraft, issueCoupon… |
| `travel_guide` | 游玩攻略 | ✅ | generateTravelGuide, getContentBlocks… |
| `order_query` | 订单查询 | ✅ | getOrders |
| `parking_pay` | 停车缴费 | ✅ | getMemberInfo → `/parking` 引导 |
| `proactive_marketing` | 主动营销 | ✅ | getCoupons / getScenicActivities / issueCoupon → 查券、餐饮零售推券（`scene_recommend` 单条合并） |
| `scenic_recommend` | 园区/演出推荐 | ✅ | getScenicActivities → 演出场次 Workflow（`scene_recommend`） |
| `invoice_service` | 发票服务 | ✅ | getOrders → 文案 +「立即开票」→ `/invoice/batch` |
| `checkin_service` | 园区打卡 | ✅ | 打卡点 →「立即打卡」→ `/checkin`（积分/小券） |
| `queue_recommend` | 虚拟排队推荐 | ✅ | 仅在园；免费包 2 卡「立即取号」/ 付费单「¥10快速排队」/ 点名项目 |
| `member_offer` | 会员权益选品 | ✅ | 等级折扣 + 可用券 + 标签选票（演示挂 `scenic_hlg`） |
| `review_service` | 服务点评 | ✅ | getOrders → ReviewCard；**「帮我写评价」**（`generateReviewDraft`，LLM≈50 字 / 离线模板） |

### RecommendEntry → Skill / 页面

| entryId | 展示标题 | 规则（摘要） | 关联 |
|---------|----------|--------------|------|
| `first_ticket_guide` | 首次购票指引 | `personaId=demo_new`；prompt「第一次购票，有推荐的门票和优惠吗？」 | `ticket_purchase` |
| `traffic_guide` | 交通指南 | `personaId ∈ demo_new/mid` | `travel_guide` |
| `new_guest_coupon` | 领取新客券 | `canClaimNewGuestCoupon=true` | 新客领券 Workflow |
| `member_coupon` | 会员专属券 | `personaId=demo_mid` | `proactive_marketing` |
| `today_route` | 今日推荐路线 | `hasPendingVisitOrder=true` **且** `inPark=true` | `travel_guide`（园内路线） |
| `parking_pay` | 停车缴费 | `inPark=true` | `parking_pay` Workflow |
| `checkin_nearby` | 园区打卡 | `inPark=true` | chat →「我要打卡」 |
| `travel_strategy` | 游玩攻略 | `personaId=demo_mid` | `travel_guide` |
| `review_service` | 服务点评 | `hasReviewableOrders=true` | `review_service` |
| `batch_invoice` | 开发票 | `hasInvoiceableOrders=true`（priority 5） | chat →「开发票」 |
| `view_orders` | 查看订单 | `personaId ∈ demo_new/mid/vip` | page `/orders?tab=1` |

> 默认来自 `recommend_entries.json`（欢迎页最多 4 条，按 priority 降序）；后台可 LocalStorage 覆盖，改 JSON 后需「恢复 JSON 默认」。

### 游游推荐 → `welcome_questions.json`

| id / 文案 | priority | 规则要点 |
|-----------|----------|----------|
| 置顶：雨天免费重玩 | 110 | 三账号；**仅** `scenic_sz_eco`（绿野）；`pinTop` |
| 置顶：海洋公园怎么玩最划算 | 100 | 空规则=始终；仅 `scenic_hy` |
| 置顶：绿野公园一日怎么逛 | 100 | 空规则；仅 `scenic_sz_eco`；prompt 不含人数 |
| 两大一小买什么票 | 10 | `demo_new` |
| 新客必玩 / 海洋必看 | 9 | `demo_new`（后者仅海洋） |
| 排队少的项目推荐 | 9 | `inPark=true` |
| 今日推荐路线 | 8 | `hasPendingVisitOrder` **且** `inPark=true` |
| 帮我规划路线 | 8 | `hasPendingVisitOrder` **且** `inPark=false` |
| 出行前要准备什么 | 7 | `demo_new/mid`；完整出行攻略（交通+入园+热门，`scope=full` 单卡） |
| 附近有什么好吃的 | 7 | `inPark=true`；副标题含优惠 |
| 景区有什么好吃的 | 6 | **仅** `demo_mid` |
| 有哪些演出项目 | 6 | **仅** `demo_mid`；演出 **general**（非当日场次） |
| 下一场演出几点开始 | 6 | `demo_vip`；当日场次 |
| 会员专属怎么买最划算 | 5 | **仅** `demo_vip` + `scenic_hlg` → `member_offer` |
| 认识海洋明星，玩个答题 | 5 | **仅** `scenic_hy` + `inPark=true` → 明星介绍+答题 CTA |
| 可以快速交停车费吗 | 2 | `inPark=true` |

> 最多展示 8 条；`rules` 数组多项为 **AND**。后台支持「附加条件（且）」编辑布尔字段。

### 2.4.1 待出行订单规则（演示版）

| 字段 | 计算方式 |
|------|----------|
| `hasPendingVisitOrder` | 存在 `paid/pending` 且 `visitDate >= 今天` 的订单 |
| `nextVisitDate` | 上述订单中 **出行日最早** 的 `visitDate` |
| `upcomingVisitOrderCount` | 待出行订单笔数 |
| `hasVisitToday` | 是否存在 `visitDate === 今天` 的待出行订单（字段目录已暴露，可配置） |
| `inPark` | `visitorState.inPark` |

实现：`src/utils/upcomingVisitOrder.ts`；攻略绑定订单：`mock/_utils.ts` → `generateTravelGuide()`。

**路线入口互斥（现行默认）**：

| 入口 | 条件 |
|------|------|
| 今日推荐路线 | 有待出行 **且** 在园 |
| 帮我规划路线 | 有待出行 **且** 不在园 |

**demo_mid Mock 订单**（`src/mock/users/demo_mid.json`）：含待出行多笔；另有已完成订单（奇趣乐园可点评/开票；海洋公园一笔 **已点评 + 未开票**）。

---

## 2.5 运行时：意图识别与对话路由

用户发消息后，**不只有 Skill 的 `triggerKeywords`**。演示版为 **Workflow 正则（代码）优先 → Skill 关键词 → LLM / 离线 Tool**。

### 2.5.1 总流程（`ChatPage.vue` → `onSend`）

> 完整对照表与「购票为何不用 LLM」说明见 [`对话意图识别与LLM分工.md`](./对话意图识别与LLM分工.md)。

```
用户输入文本
    │
    ├─① Workflow 层（代码正则，最高优先级；ChatPage 执行顺序摘要）
    │     新客领券 / 停车缴费 / 虚拟排队 / 明星介绍 / 演出场次
    │     游玩攻略（travel_guide：full / traffic / entry / in_park / recommend）
    │     购票 / 会员权益选品（member_offer） / 主动营销（餐饮零售）
    │     发票 / 打卡 / 点评 / 订单查询
    │
    ├─② 未命中 → sendChatMessage
    │     skillStore.resolveSkill(text)     // skills.json triggerKeywords
    │     → LLM + Tool Calling（或离线 fallback）
    │
    └─③ formatters → 业务卡片 / 单条消息框回复
```

**要点：** 新客领券 **无独立 Skill**，不读 `triggerKeywords`；命中 Workflow 后 **不走 LLM**；`member_offer` 优先于泛查券营销。

### 2.5.2 四层配置对照

| 层级 | 作用 | 配置方式 | 主要文件 |
|------|------|----------|----------|
| **Workflow 意图** | 固定流程（领券 / 购票 / 攻略 / 订单） | **代码正则** | `src/utils/newGuestCoupon.ts`、`src/ai/workflow/*.ts` |
| **Skill 路由** | LLM 用哪个 Skill、哪些 Tool | **`triggerKeywords`**（后台可改） | `skills.json`、`src/ai/skills/router.ts` |
| **欢迎页入口** | 快捷服务 / 游游推荐 **是否展示** | **规则引擎** | `recommend_entries.json`、`welcome_questions.json`、`field_catalog.json` |
| **欢迎页标题文案** | 欢迎区 title / subtitle / body | JSON（按 persona） | `welcome_templates.json` |

### 2.5.3 Workflow 意图（代码正则）

| 场景 | 判断函数 | 示例说法 | 结果 |
|------|----------|----------|------|
| 新客领券 | `isNewGuestCouponClaimIntent` | 领新客券、领新人券 | 单券 Workflow |
| 交通指南（单篇） | `isTrafficGuideIntent` | 交通指南、怎么去、怎么到景区、停车位置 | `getContentBlocks(traffic)` |
| 入园提醒（单篇） | `isEntryNoticeIntent` | 入园须知、门票怎么使用、几点可以入园、要带什么 | `getContentBlocks(entry_notice)` |
| **园内路线推荐** | `isInParkRouteIntent` | 在园区内、现在先玩哪里、根据排队推荐今日路线 | `generateTravelGuide(scope=in_park)`，**不含**交通/入园 |
| 组合游玩攻略 | `isFullTravelGuideIntent` 等 | 游玩攻略、按订单做攻略、近期出游 | `generateTravelGuide(scope=full)` |
| 项目/路线推荐 | `isRecommendTravelGuideIntent` | 推荐游玩项目和路线、不需要交通/入园 | `generateTravelGuide(scope=recommend)` |
| Skill 语义路由（P0-2） | `resolveSkillRoute` + `skillWorkflowGate` | 关键词未命中 → LLM；confidence≥0.8 可进 Workflow |
| 攻略子意图（P0-3） | `resolveTravelGuideIntentRoute` | 泛化说法 | LLM 细分 traffic / entry / full |
| 购票槽位（P0-1） | `resolvePurchaseSlotsFromMessage` | 人数+日期口语 | **LLM 优先**（带 session 上下文），失败/无 Key 时正则兜底 |
| 购票 | `shouldRunTicketWorkflow` | 两大一小、买票、购票、买门票（**不含**裸「门票」FAQ） | 购票 Workflow；与明确攻略冲突时攻略优先 |
| 订单查询 | `shouldRunOrderQueryWorkflow` | 查订单、我的订单 | 订单 Workflow |
| **虚拟排队** | `shouldRunQueueRecommendWorkflow` | 虚拟排队/排队少的项目（免费包）；快速排队（付费单）；点名项目名 | `queue_recommend`；**仅在园**；优先于园内路线攻略 |
| 园区打卡 | `shouldRunCheckinWorkflow` | 打卡、签到、我要打卡 | `checkin_service` |

**修改交通/入园说法：** 改 `src/utils/travelGuideIntent.ts`；泛化说法子意图见 `src/ai/nlu/resolveTravelGuideIntent.ts`（P0-3）。  
**修改园内路线说法：** 改 `isInParkRouteIntent`（对齐 welcome「今日路线」prompt；与虚拟排队互斥）。  
**修改虚拟排队说法：** 改 `src/utils/queueRecommendIntent.ts`。  
**修改新客领券说法：** 改 `src/utils/newGuestCoupon.ts` 内正则。  
**修改购票/攻略说法：** 改对应 `src/ai/workflow/*.ts` 与 `src/ai/nlu/*`。

### 2.5.4 欢迎页 chip → 攻略子意图（示例）

| persona | chip 文案 | prompt（节选） | 子意图 | 返回内容 |
|---------|-----------|----------------|--------|----------|
| demo_vip | 现在先玩哪里？ | 我现在在园区内…推荐今日路线 | `in_park` | 攻略正文 + 推荐项目（**无**交通/入园） |
| demo_mid | 交通指南 | 请告诉我…交通指南、停车 | `traffic` | 单篇 ContentCard |
| demo_mid | 按订单做游玩攻略 | 根据待出行订单做攻略 | `full` | 交通 + 入园 + 攻略 + 项目 |

文案维护：`welcome_templates.json`；识别逻辑：`travelGuideIntent.ts` + P0-3 NLU。

### 2.5.5 Skill 关键词（`triggerKeywords`）

- 路由：`src/ai/skills/router.ts`（消息包含 keyword 即命中，更长优先）
- 后台：**业务配置 → Skill → 触发关键词**
- 仅 **未命中 Workflow** 时 Skill 主导 LLM 链路

### 2.5.6 离线 / LLM 兜底

`src/ai/llm.ts`：`runOfflineToolFallback` 按正则选 Tool；领券结果合并为 **单条消息框**（与 Workflow 一致）。

**相关代码：** `ChatPage.vue` → `onSend`；`src/ai/skills/router.ts`；`skillStore` ← `businessConfigStore`

---

## 2.6 与内容数据的关系

| 层级 | 配置 | 含攻略/提醒正文 |
|------|------|----------------|
| Skill / 入口 | skills.json、recommend_entries.json | 否 |
| 正文 | content/blocks.json、activities.json | 是 |

详见 [`内容数据配置说明.md`](./内容数据配置说明.md)。

---

## 3. 配置 Schema

### 3.1 元数据 — `field_catalog.json`

规则字段：`fieldId`、`valueType`、`ruleEligible`；标签：`tagId` + `demoPersonas`。

**推券相关字段：**

| fieldId | 含义 |
|---------|------|
| `canClaimNewGuestCoupon` | 是否可展示/领取新客券（身份 + 窗口 + 未领过） |
| `hasNewGuestCoupon` | 账户是否有 **可用** 新客专享券 |

### 3.2 Skill — `skills.json`

Phase A 已支持后台编辑：enabled、triggerKeywords、tools、promptAddon。

**触发关键词规则（后台 `/admin/business`）：**

| 规则 | 说明 |
|------|------|
| 子意图展示 | 只读目录 `skill_intent_catalog.json`，Skill 详情内展示各子意图及识别词 |
| 子意图词不可删 | 自动并入 `triggerKeywords`，标签带 🔒 |
| 全局唯一 | 同一关键词只能属于一个 Skill，保存时报错 |
| 超集约束 | `triggerKeywords` 须包含该 Skill 全部子意图关键词 |

校验实现：`src/utils/skillKeywordValidation.ts`；目录：`src/utils/skillIntentCatalog.ts`。

### 3.3 推荐入口 — `recommend_entries.json`

```typescript
interface RecommendEntryConfig {
  entryId: string
  title: string
  icon: string
  target: 'chat' | 'page' | 'h5' | 'mini_program'
  targetPath?: string
  skillId?: string
  promptHint?: string
  priority: number
  enabled?: boolean
  rules: RuleExpression[]
}
```

### 3.4 卡片展示 — `card_views.json`

只读展示；Phase C 接入 formatter 显隐。

### 3.5 LocalStorage Override

| Key | 内容 |
|-----|------|
| `scenic_admin_business_override` | `{ skills?, recommendEntries? }` |

---

## 4. 轻量配置后台 `/admin/business`

| Tab | 能力 |
|-----|------|
| **Skill 场景** | 列表开关、编辑关键词 / 工具 / promptAddon |
| **欢迎页入口** | 入口编辑、规则预览、字段目录只读 |

规则预览：三演示账号分屏（新客 / 中级 / 高级），展示 RuleContext + 命中入口列表。

---

## 5. 运行时数据流

### 5.1 推荐入口

```
recommend_entries.json → businessConfigStore → resolveActiveRecommendEntries(personaId)
  → Chat 欢迎区「为您推荐」chips ✅（最多 4 条）
```

### 5.2 Skill

```
skills.json → skillStore → routeSkillByKeywords → Workflow / LLM + Tools
```

### 5.3 欢迎页「游游推荐」与标题文案

```
welcome_questions.json → evaluateRules → ChatPage「游游推荐」（最多 8 条）
welcome_templates.json → 标题区 title / subtitle / body（按 persona）
```

与 RecommendEntry **同一规则模型**：`快捷服务` / `游游推荐` 均用 `rules` + `field_catalog`；演示账号仅为规则字段选项。

---

## 6. API 清单

| 接口 | 用途 |
|------|------|
| `GET /api/admin/field-catalog` | 字段 / 标签元数据 |
| `GET /api/admin/recommend-entries` | 含 rules 的快捷服务配置 |
| `GET /api/admin/welcome-questions` | 含 rules 的游游推荐配置 |
| `GET /api/assistant/skills` | Skill 默认 JSON |
| `POST /api/coupons/issue` | 发券；`purpose: claim \| purchase` |
| `GET /api/coupons` | 用户券列表 |
| `POST /api/demo/reset` | 重置演示 Mock 数据 |

---

## 7. 文件清单

```
src/utils/newGuestCoupon.ts      # 领券意图、规则字段、文案常量
src/ai/workflow/newGuestCoupon.ts
src/mock/assistant/
  ├── field_catalog.json
  ├── recommend_entries.json
  ├── welcome_questions.json
  ├── welcome_templates.json
  └── skills.json
src/pages/admin/AdminBusinessPage.vue
mock/rules.ts
```

---

## 8. Phase C（欢迎页 1+2）✅

- Chat 欢迎区接入 RecommendEntry chips（「快捷服务」，最多 4 条）
- 「游游推荐」与快捷服务同一套 `rules` 模型（`welcome_questions.json`）
- `welcome_templates` 仅保留标题区文案（按 persona）
- 移除欢迎页 dock / 聊天 footer `quickActions`
- `today_route` → `travel_guide` + 园内 prompt；`member_coupon` 改自然语言 prompt

**待做（Phase C 之外）**

- Card View 显隐 / maxItems
- Skill `phase` 路由、`triggerRules`
- `proactive_marketing` Skill 启用（泛推券，与新客领券 Workflow 并存）

---

## 9. 生产演进

```
Demo LocalStorage  →  运营后台 POST  →  配置中心 DB
field_catalog      →  用户画像服务字段注册表
triggerRules       →  规则引擎 / 策略平台
```

---

## 10. 验证步骤

### 10.1 业务配置后台

1. **我的 → 业务场景配置 → 欢迎页入口**
2. 三账号分屏：demo_new 未领券时 **显示**「领取新客券」；领券或清除演示后 **隐藏**
3. demo_mid「交通指南」；demo_vip「现在先玩哪里」→ 园内路线卡**不含**交通/入园区块
4. 改 Skill 关键词后，对话应影响 `routeSkillByKeywords`

### 10.2 新客领券（验收）

| # | 账号 | 操作 | 预期 |
|---|------|------|------|
| 1 | demo_new | 欢迎页（未领券 / 清除演示后） | 显示「新客券怎么领取？」「领新客券」 |
| 2 | demo_new | 领券后再进欢迎页 | **不显示**领券入口 |
| 3 | demo_new | 首次领券 | 单条消息：文案 + 券卡（可使用）+「点击查看」 |
| 4 | demo_new | 重复领券（available） | 「您已领取过新客券…」+ 券卡 + 按钮，不重复写入 |
| 5 | demo_new | 重复领券（used/expired） | 同上，券卡状态为已使用/已过期 |
| 6 | demo_mid / demo_vip | 发领券 prompt | 不符合条件，**无卡片** |
| 7 | demo_new | 注册超 `claimWindowDays` 未领 | 超期说明，**无卡片**，欢迎页无入口 |
| 8 | demo_new | 「两大一小有优惠吗」 | 购票 workflow 内推券（`purpose=purchase`） |

超期演示：改 `demo_new.json` 的 `registeredAt` 为 31 天前，**重启 dev**（勿点「清除演示」）。

### 10.3 园内路线（验收）

| # | 账号 | 操作 | 预期 |
|---|------|------|------|
| 1 | demo_vip | 点「现在先玩哪里？」/「今日推荐路线」类 prompt | GuideCard 仅 **日程建议 + 推荐项目**，无交通/入园 |
| 2 | demo_vip | 发「我现在在园区内，请根据当前位置和排队情况推荐今日路线。」 | 同上；过程面板显示攻略 Workflow |

### 10.4 虚拟排队（验收）

| # | 账号 | 操作 | 预期 |
|---|------|------|------|
| 1 | demo_vip | 「虚拟排队」/「排队少的项目」 | 2 免费项目卡，各有「立即取号排队」→ `/queue/take` |
| 2 | demo_vip | 「快速排队」 | 极限过山车 +「¥10元快速排队」→ `/queue/pay` |
| 3 | demo_vip | 「亲子漂流」/「极限过山车」 | 单卡 + 对应免费/付费 CTA |
| 4 | demo_new / mid | 同上话术 | 提示入园后才可虚拟排队 |

### 10.5 欢迎天气（验收）

| # | 账号 | 操作 | 预期 |
|---|------|------|------|
| 1 | 任意 | 打开 /chat 欢迎态 | Hero 气泡「我是游游」上方可见「今天景区天气 26°C，多云，适合出游」 |

---

## 11. 推券场景（新客领券与主动营销）

> 原 [`推券功能-需求细化.md`](./推券功能-需求细化.md) 已并入本章。关联落地计划 §8.1。

### 11.1 目标与边界

**本期已实现：**

- 新客领券 Workflow + 单条消息框（文案 + CouponCard +「点击查看」）
- 购票途中推券（`ticket_purchase` workflow；明确购票意图时在追问人数/日期阶段即可提前推 `cp_prod_purchase`）
- RecommendEntry / welcome 领券入口与 `canClaimNewGuestCoupon` 过滤
- **主动营销 Workflow**（`proactive_marketing`）：查券；询问餐饮/美食、零售/伴手礼时推荐活动并推场景券（`cp_prod_dining_guide` / `cp_prod_retail_guide`）；非门票下单意图顺势推券

**本期不做 / P3 预留：**

- **P3**：欢迎态主动气泡推券（H2）；游中地理围栏 / 离园推券（H5）；离园主动推送（发票/点评/复游券）；OCR 小票积分对话接入
- ~~与 `scenic_recommend` 合并为组合推荐~~ → 已拆独立 Skill **`member_offer`**
- **不做**：快递周边发券；微信卡包/订阅消息（演示范围外）

**与购票发券的关系（已定稿选项 A）：** 新客领券（欢迎入口）与购票途中推券为 **两套场景**，可共用 `cp_prod_new`，入口与触发时机分离。

### 11.2 场景拆分

| 维度 | **新客领券** | **主动营销** |
|------|-------------|-------------|
| **目的** | 新客主动领取固定新客券 | 对话中识别 **查券 / 餐饮零售 / 下单意图** 时顺势推券 |
| **绑定券** | 固定 `cp_prod_new` | 查券可领预览；餐饮 `cp_prod_dining_guide`；零售 `cp_prod_retail_guide`；购票 `cp_prod_purchase` |
| **触发** | welcome「为您推荐」/「猜你想问」；RecommendEntry `new_guest_coupon` | 「有什么券」「推荐美食」「伴手礼」「想买/下单」等；购票正则走购票 Workflow |
| **频控** | 每人仅 1 次；`issueCoupon(purpose=claim)`；不可领则隐藏入口 | 同产品可用券不重复发；购票会话内 `marketingIssued` |
| **Skill** | 无；`runNewGuestCouponWorkflow` | `proactive_marketing` + **购票 workflow** |
| **意图** | 代码正则 `isNewGuestCouponClaimIntent` | `resolveProactiveMarketingScene` + 购票正则 |

**欢迎页过滤：** `canClaimNewGuestCoupon === false`（非新客 / 超领取窗口 / 已领过任意状态）时隐藏 `new_guest_coupon`、`claim_new_coupon`。

**演示：** `cp_prod_new.claimWindowDays` 默认 **30**（`coupon_products.json`）。Mock 启动/清除演示时，若注册已过期且未领券，会将 `registeredAt` 设为「昨天」。

### 11.3 已实现触发入口

| ID | 入口 | 状态 |
|----|------|------|
| H1 | RecommendEntry「领取新客券」 | ✅ `canClaimNewGuestCoupon=true` |
| H3 | 对话下单意图 → 购票 workflow 推券 | ✅ 推荐票种前即可提前推券 |
| H4 | welcome「领取新客券」chip / 猜你想问 | ✅ 已领则过滤 |
| H5a | 餐饮/美食询问 → **单条** `scene_recommend`（券 + 店铺卡） | ✅ **仅在园**推场景券；非在园只推荐店铺不发券 |
| H5b | 零售/伴手礼询问 → **单条** `scene_recommend`（券 + 店铺卡） | ✅ 同上（仅在园推券） |
| H5c | 非门票下单意图 → 顺势推券 | ✅ |
| H2 | 欢迎态主动气泡 | ❌ **P3 预留** |
| H5 | 游中地理围栏 / 离园推券 | ❌ **P3 预留** |

### 11.4 发券规则（已实现）

| ID | 规则 |
|----|------|
| R3 | 已有同 title 新客券（任意 status）→ 不重复发，提示已领取并展示券卡 |
| R3b | 非新客（demo_mid/demo_vip）→ 不符合条件，无卡片 |
| R3c | 新客超 `claimWindowDays` 未领 → 超期说明，无卡片 |

### 11.5 券产品清单

| productId | 名称 | 条件 | 备注 |
|-----------|------|------|------|
| `cp_prod_new` | 新客专享 20 元券 | 满 200 可用 | `claimWindowDays`: 30 |
| `cp_prod_parking` | 停车 5 元券 | — | |
| `cp_prod_purchase` | 购票10元券 | 满 200 可用 | 老用户优先；无可用新客券时主动营销 |
| `cp_prod_vip` | 黄金会员 50 元券 | 会员专享 | |
| `cp_prod_dining_guide` | 美食券 | 满 69 可用 | 餐饮询问 / 下单意图 |
| `cp_prod_retail_guide` | 商品券 | 满 128 可用 | 零售询问 / 下单意图 |

### 11.6 新客领券意图（详见 §2.5.3）

- **配置位置：** `src/utils/newGuestCoupon.ts` → `isNewGuestCouponClaimIntent`
- **形式：** TypeScript 正则，**不是** `skills.json` 的 `triggerKeywords`
- **匹配示例：** 领取、领券、领新X券、新客券、新人券、怎么领；或（新客/新人 + 券）
- **排除：** 含两大一小、买票、套票等 → 走购票 Workflow
- **与 getCoupons：** 泛查券走 Skill/LLM；误走 getCoupons 的领券说法，离线兜底仍按已领取单条消息回复

### 11.7 回复话术（已定稿）

| 场景 | 文案 | 券卡片 | 「点击查看」 |
|------|------|--------|-------------|
| A. 新领取成功 | 新客专享券已发放到您的账号。 | ✅ 可使用 | ✅ → `/coupon` |
| B. 已领取过 | 您已领取过新客券，不能重复领取噢~ | ✅ 按状态 | ✅ |
| C. 非新客 | 抱歉，您暂不符合新客专享券的领取条件。\n该优惠仅限新注册会员领取。 | ❌ | ❌ |
| D. 超领取期 | 抱歉，您暂不符合…\n新客券需在注册后 **N** 天内领取… | ❌ | ❌ |

购票 workflow 推券：卡片 `action=use`。

### 11.8 CouponCard UI（已实现）

| 功能 | 状态 |
|------|------|
| 面额、条件、有效期 | ✅ |
| 状态角标：可使用 / 已使用 / 已过期 | ✅ |
| 类型标签（代金券）+ 状态标签并排 | ✅ |
| 单条消息框：文案 + 卡片 +「点击查看」 | ✅ |
| ToolProcessPanel 发券步骤 | ✅ |

### 11.9 RecommendEntry 衔接

| entryId | 标题 | promptHint | 规则 |
|---------|------|------------|------|
| `new_guest_coupon` | 领取新客券 | 请帮我领取新客专享优惠券 | `canClaimNewGuestCoupon=true` |
| `member_coupon` | 会员专属券 | 有什么会员专属优惠 | `persona=demo_vip` |

`new_guest_coupon` **不**绑定 `proactive_marketing`。

购票 workflow 主动营销：无可用新客券时发 **购票10元券**（`cp_prod_purchase`），计入最高抵扣统计；有可用新客券则不推送。

---

## 12. 智能购票场景（多轮 Workflow）

> 首次购票指引与老客购票共用 **`ticket_purchase` Skill + 多轮 Workflow**；会话状态由 `purchaseStore` 维护。

### 12.1 对话步骤

| 步骤 | 动作 | 说明 |
|------|------|------|
| 1 | 问人数 | 几人出行、是否有老人/小孩；可分步收集 |
| 2 | 问日期 | 具体日期或模糊说法（这周末、下周） |
| 3 | 解析日期 | `visitDateParser.ts`：模糊时段取距今天最近且 **≠ 当天**；**下周末/下个周末** 优先于「周末」 |
| 4 | 主动营销 | 无 **可用** 新客券时 `issueCoupon(cp_prod_purchase)` |
| 5 | 推荐产品 | 可闭环 → 单 SKU 推荐卡；不可闭环 → **兜底话术 + 购票列表按钮**（见 §12.5） |
| 6 | 确认下单 | 仅 `recommend` 步：点击推荐卡按钮 → `createOrderDraft` → `/order/submit` |
| 7 | 下单页 | `/order/submit` 选手实名出行人 → Mock 支付 → **`/orders?tab=1`**（待出行/已完成 Tab） |

### 12.2 选品规则（`matchPartyToProducts`）

| 人数 | 结果 | 说明 |
|------|------|------|
| 2 大 1 小 | `single` | 家庭套票 `tkt_family` |
| N 大 0 小 | `single` | 成人票 `tkt_adult` × N |
| 3 大 1 小、1 大 1 小 等 | `multi_product` | 需多 SKU 组合，助手 **不支持购物车** |
| 含老人且无老人票 | `no_product` | 提示无符合规则产品 |

**卡片推荐标签：** `tickets.json` → `recommendLabel`；留空则不显示。

### 12.5 无法闭环兜底

| 分支 | 对话提示 | UI |
|------|----------|-----|
| `no_product` | 当前没有符合规则的可售产品 | `ticket_fallback` 卡 +「去购票列表选购」→ `/tickets` |
| `multi_product` | 查到多款产品，但不支持购物车合并下单 | 同上；可展示参考组合明细 |

- **专用券：** `cp_prod_manual` 购票优享 5 元券（与停车 `cp_prod_parking` 等同为 5 元但 **券产品 ID 不同**，互不冲突）
- **识别与去重：** 券实例带 `couponProductId`；`findFallbackPurchaseCoupon()` / Mock `issueCoupon` 均按 **产品 ID** 判定，不按金额或标题
- **展示：** 进入兜底时 **始终展示** 优享券卡片；账号尚无可用 `cp_prod_manual` 时调用 `issueCoupon` 新发，已有则复用展示、不重复入库
- **与购票 10 元券区分：** 10 元券（`cp_prod_purchase`）仅在 `single` 推荐成功路径推送；5 元优享券仅在 fallback 路径
- **关键文件：** `partyProductMatch.ts`、`purchaseFallbackCoupon.ts`、`TicketFallbackCard.vue`、`TicketsPage.vue`

### 12.3 优惠券逻辑（可闭环路径）

- **主动营销：** `shouldPushPurchaseMarketingCoupon()` — 账号无新客券，或新客券已用/已过期 → 发购票10元券
- **抵扣计算：** `pickBestCoupon()` — 账号可用券（含刚发的营销券）取最高抵扣；优惠价 = 原价 − 抵扣

### 12.4 关键文件

| 文件 | 作用 |
|------|------|
| `src/store/purchaseStore.ts` | 购票会话 step / 人数 / 日期 |
| `src/ai/workflow/ticketPurchase.ts` | 多轮 Workflow |
| `src/utils/ticketPartyParser.ts` | 人数解析 |
| `src/utils/visitDateParser.ts` | 日期解析 |
| `src/utils/partyProductMatch.ts` | 选品三分支：single / no_product / multi_product |
| `src/utils/purchaseFallbackCoupon.ts` | 兜底 5 元券推送判定 |
| `src/utils/purchaseMarketing.ts` | 购票10元券推送判定 |
| `src/utils/couponDiscount.ts` | 最高可用券 |
| `src/components/chat/cards/TicketCard.vue` | 推荐产品信息 |
| `MessageBubble`（ticket 气泡底部） | 「确认并生成订单」按钮 |
| `src/pages/order/OrderSubmitPage.vue` | 选手实名出行人 |

### 12.6 验收

| # | 操作 | 预期 |
|---|------|------|
| 1 | demo_new 首次购票指引 | 分步问人数 → 日期 → 推荐 |
| 2 | 2 大 0 小 | 成人票 ×2，优惠价 598−抵扣 |
| 3 | 2 大 1 小 | 家庭套票 669 |
| 4 | demo_new 已有可用新客券 | **不**推购票10元券，抵扣用新客券 |
| 5 | demo_mid 购票 | 推购票10元券；可能与餐饮券比取最高 |
| 6 | 确认并生成订单 | 跳 `/order/submit`，需选 N 人后才能支付；支付成功 → `/orders?tab=1` |
| 7 | **3 大 1 小** + 日期 | 兜底话术 + 参考组合 + 跳 `/tickets`；展示并发放 5 元优享券（`cp_prod_manual`） |
| 8 | 重复触发兜底且已有 `cp_prod_manual` | **不重复发券**，仍展示优享券卡片 |
| 9 | demo_mid 欢迎页「查看订单」 | 跳 `/orders?tab=1`，默认待出行/已完成 Tab |

### 12.7 批量开票假页（Q6）✅

| 项 | 说明 |
|----|------|
| 入口 | 对话「开发票」/快捷推荐「开发票」→ Workflow 引导卡「立即开票」→ `/invoice/batch`（**不再**由 RecommendEntry 直跳） |
| 页面 | 顶栏/底栏与 H5 内容同宽对齐；多选 30 天内已完成且未开票订单 →「提交批量开票」 |
| API | `POST /api/invoice/batch` `{ orderIds: string[] }` → 更新 Mock 订单 `invoiceStatus=applied` |
| 单笔 | `/invoice` 列表仍可用；`POST /api/invoice/apply` 单笔后跳 `/invoice/external` |
| 演示账号 | **demo_vip**：`ORD20260715001`、`ORD20260710001`（30 天内 completed + `invoiceStatus=none`） |

| # | 操作 | 预期 |
|---|------|------|
| 10 | demo_vip 快捷推荐「开发票」 | 进入聊天并发送「开发票」→ 见 §12.9 |
| 11 | 点「立即开票」进入批量页 | 默认 2 笔可勾选，提交后 Toast 并回 `/invoice` |
| 12 | 再次进入批量页 | 已提交订单不再出现 |

### 12.8 停车缴费对话引导 ✅

| 项 | 说明 |
|----|------|
| Skill | `parking_pay`（enabled）；关键词：停车缴费、停车费、交车费、车牌等 |
| Workflow | `runParkingPayWorkflow` → 查绑定车牌 → 文案 + `page_guide` 卡片 → `/parking` |
| 与交通区分 | 「停车位置/停车场在哪」→ `travel_guide`；「交停车费」→ `parking_pay` |

| # | 操作 | 预期 |
|---|------|------|
| 13 | demo_mid「我要交停车费」 | **单条**引导气泡（文案+卡片）；已绑车牌 → 按钮跳 `/parking?mode=pay` 直达缴费 |
| 14 | demo_new「停车缴费」 | 单条引导气泡；按钮「前往绑定车牌并缴费」→ `/parking?mode=bind` |
| 15 | 「停车位置在哪」 | 仍走交通指南 ContentCard，不进缴费 Workflow |

### 12.9 发票服务对话引导 ✅

| 项 | 说明 |
|----|------|
| Skill | `invoice_service`（`enabled: true`）；关键词：发票、开票、报销、开发票、批量开票 |
| Workflow | `runInvoiceServiceWorkflow` → `getOrders` → `filterInvoiceableOrders` |
| 有可开票 | 单条气泡：`您当前有X笔订单可以申请开票` + 按钮「立即开票」→ **一律** `/invoice/batch` |
| 无可开票 | 纯文案：`当前没有可申请开票的订单。`（无引导卡） |
| 意图 | `shouldRunInvoiceWorkflow`（`invoiceIntent.ts`）；P0-2 `invoice_service` |
| 与欢迎入口 | `batch_invoice` → `target: chat` + `promptHint: "开发票"`，与对话同一条 Workflow |

| # | 操作 | 预期 |
|---|------|------|
| 16 | demo_vip「开发票」/「我要开发票」 | 文案「您当前有2笔…」+「立即开票」→ 批量页 |
| 17 | 无可开票订单（或已全部申请） | 「当前没有可申请开票的订单。」 |

### 12.10 演出项目推荐（`scenic_recommend`）✅

| 项 | 说明 |
|----|------|
| Skill | `scenic_recommend`；意图 `shouldRunShowScheduleWorkflow`（`showScheduleIntent.ts`） |
| Workflow | `runShowScheduleWorkflow` → `getScenicActivities` → **单条** `scene_recommend` |
| 优先级 | **高于** `travel_guide`（避免「演出项目推荐」被「项目推荐」攻略抢走） |
| 当日 | 「今天/今日有演出吗」「下一场」等 → `dayKind=today` |
| 指定日 | 「明天/后天」→ 最早一场推荐 |
| 非当日推荐 | 「有哪些演出项目推荐」「演出推荐」等未点名日期 → `dayKind=general` |
| 游游推荐 | 中级「有哪些演出项目？」→ general；VIP「下一场演出几点开始」→ today |
| 点名游乐项目 | 「过山车」等简称模糊匹配虚拟排队项目（优先于演出/攻略） |

| # | 操作 | 预期 |
|---|------|------|
| 18 | 「请介绍一下园区有哪些演出项目推荐？」 | 单条演出推荐（general，非订单攻略） |
| 19 | 「今天有演出吗」 | 单条；按当日剩余场次 / 已全部结束区分 |
| 20 | 奇趣乐园「过山车」 | 命中「极限过山车」虚拟排队 / 项目卡 |

### 12.11 会员权益选品（`member_offer`）✅

| 项 | 说明 |
|----|------|
| Skill | `member_offer`；意图 `shouldRunMemberOfferWorkflow` |
| Workflow | `runMemberOfferWorkflow`：等级折扣（黄金 95 折）+ `pickBestCoupon` + 标签选票 → Coupon 卡 + Ticket 卡 |
| 边界 | 优先于 `proactive_marketing` 查券；已报「两大一小」等购票槽位仍走 `ticket_purchase` |
| Mock | 票/券挂 `scenic_hlg`；`cp_prod_vip` / `cpn_vip_001`；家庭套票/年卡带「会员」标签 |
| 游游推荐 | `vip_member_offer`：demo_vip + 奇趣乐园，priority 5 |

| # | 操作 | 预期 |
|---|------|------|
| 21 | demo_vip @ 奇趣乐园点「会员专属怎么买最划算？」 | 券卡 + 家庭套票/年卡组合价，可确认下单 |

### 12.12 答题互动（挂靠演出 / 明星）✅

| 项 | 说明 |
|----|------|
| 入口 | **不做**「我想答题」；仅：① 海洋演出结果含海豚表演；② 「企鹅」「白鲸」明星介绍 |
| UI | 业务卡与邀请 **同条消息**；点「开始答题」后 QuizCard 一题一点选 |
| 规则 | 答错立即结束；全对发 `cp_prod_hy_quiz` + 积分；`completedQuizIds` 后不再出 CTA |
| Mock | `stars.json`、`quiz.json`（3 套）均挂 `scenic_hy`；`act_hy_001.quizId` |
| 游游推荐 | `hy_star_quiz`：海洋公园 + 在园，priority 5 |

| # | 操作 | 预期 |
|---|------|------|
| 22 | 海洋公园「今天海豚表演几点」 | 场次卡底部答题邀请 |
| 23 | 「介绍白鲸」→ 全对 | 介绍卡+邀请 → 领券积分；再问无 CTA |
| 24 | 答题中故意答错 | 提示结束，无奖励 |

---

## 13. 变更记录

| 日期 | 摘要 |
|------|------|
| 2026-07-23 | 游游推荐展示上限 6 → 8（`MAX_WELCOME_RECOMMEND`） |
| 2026-07-22 | 答题互动：海豚表演/企鹅/白鲸挂靠 CTA；海洋 3 套题；游游推荐在园+海洋 priority 5；无「我想答题」冷启动 |
| 2026-07-22 | 新增 `member_offer`：等级+折扣+券+标签选品；游游推荐 VIP+奇趣乐园 priority 5；票券 Mock 挂 `scenic_hlg` |
| 2026-07-22 | 范围调整：H2 主动气泡、地理围栏/离园推券、离园主动推送、OCR 小票积分 → **P3 预留**；快递周边发券 → **不做** |
| 2026-07-22 | 欢迎/快捷默认大改：深圳仅绿野启用；置顶按园；出行前合并 full 攻略；今日路线↔规划路线按在园互斥；演出 general 与攻略分流；餐饮零售非在园不推券；后台模块折叠 + 规则 AND；`hasVisitToday` 入字段目录 |
| 2026-07-22 | 欢迎 Hero：当日景区天气本地 Mock（`scenicWeather.ts`，在「我是游游」上方） |
| 2026-07-22 | 游中虚拟排队 `queue_recommend`：在园；免费包/付费单/点名（含简称匹配）；每卡 CTA → `/queue/take` `/queue/pay` |
| 2026-07-22 | 快捷服务 + 游游推荐默认配置同步自后台 LocalStorage（均按演示账号规则） |
| 2026-07-21 | 游游推荐改为与快捷服务同一套规则模型（`welcome_questions.json` + `evaluateRules`；演示账号为规则选项） |
| 2026-07-21 | 园区打卡 MVP：`checkin_service` + `/checkin` + spots Mock；对话引导；在园快捷入口 |
| 2026-07-21 | 服务点评：ReviewCard / `/review` 增加「帮我写评价」（`generateReviewDraft`，LLM≈50 字 / 离线模板；结合景区关键词与已选标签） |
| 2026-07-21 | 发票：对话统一「X笔 + 立即开票→批量页」；0 笔固定文案；快捷「开发票」改 chat 发「开发票」；demo_vip 补 2 笔可开票订单；批量页顶栏/底栏与 H5 同宽 |
| 2026-07-21 | 演出：场次回复合并为一条 `scene_recommend`；「演出推荐」命中非当日（general）；区分今日剩余场次 |
| 2026-07-21 | 餐饮/零售营销回复合并为 `scene_recommend`（券+店铺卡单条） |
| 2026-07-21 | 后台欢迎页入口：列表按 priority 降序；修复编辑规则时 watch 覆盖条件值；personaId 多选 / in 规则可编辑 |
| 2026-07-21 | 快捷服务默认配置同步自后台 LocalStorage（按演示账号规则；移除 nearby_offers） |
| 2026-07-21 | 购票/攻略分流：收窄「门票」购票意图；入园 FAQ 与游玩推荐优先攻略；购票会话不再劫持明确攻略 |
| 2026-07-18 | 主动营销：餐饮/零售询问与非门票下单意图推场景券；购票意图在追问人数/日期阶段提前推券 |
| 2026-07-07 | §12.9 发票对话 Workflow（`invoice_service` + PageGuideCard） |
| 2026-06-30 | demo_mid 三笔待出行订单；待出行按真实日期过滤；欢迎页「今日出行」；快捷服务增加停车缴费 |
| 2026-06-23 | §12.8 停车 Skill 对话引导 + PageGuideCard |
| 2026-06-23 | §12.7 批量开票假页 `/invoice/batch` + RecommendEntry 跳转修正 |
| 2026-06-23 | 兜底优享券按 `couponProductId` 识别；欢迎「查看订单」→ `/orders?tab=1`；支付成功同 Tab |
| 2026-06-24 | §12.5 购票无法闭环兜底（3大1小等）+ 购票优享 5 元券 + `/tickets` |
| 2026-06-23 | §12 智能购票多轮 Workflow；购票10元券主动营销 |
| 2026-06-23 | 合并原「推券功能-需求细化」；重写 §2.5 意图路由；更名本文档 |
| 2026-06-19 | Phase B RecommendEntry；新客领券 UX、canClaimNewGuestCoupon |
| 2026-06-18 | Phase A Skill / field_catalog 初版 |
